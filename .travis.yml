language: node_js
dist: trusty
sudo: required
node_js:
  - "8"
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - truffle test
after_script:
  - npm run coverage && cat coverage/lcov.info | coveralls
